<!DOCTYPE html!>
<html>
  <head>
    <title>{{mod.name}} items</title>
    <link rel="stylesheet" href="../style.css">
    <script src="https://kryogenix.org/code/browser/sorttable/sorttable.js"></script>
  </head>
  <body>
    {% if mod.cards %}
    <h1>{{mod.name}} cards</h1>
    <table class="sortable">
      <tr>
        <th>Name</th>
        <th>Image</th>
        <th>Upgraded image</th>
        <th>Rarity</th>
        <th>Type</th>
        <th>Cost</th>
        <th>Description</th>
      </tr>
      {% for card in mod.cards %}
      <tr>
        <td>{{ card.name }}</td>
        <td><a href="../{{card.card.color}}/{{ card.relImage }}"><img src="../{{card.card.color}}/{{ card.relImage }}" height="298"></a></td>
        <td>{% if card.upgrade %}<a href="../{{card.card.color}}/{{ card.upgrade.relImage }}"><img src="../{{card.card.color}}/{{ card.upgrade.relImage }}" height="298"></a>{% endif %} </td>
        <td>{{ card.rarity }}</td>
        <td>{{ card.type }}</td>
        <td>{{ card.costAndUpgrade }}</td>
        <td>{{ card.textAndUpgrade }}</td>
      </tr>
      {% endfor %}
    </table>
    {% endif %}

    {% if mod.relics %}
    <h1>{{mod.name}} relics</h1>
    <table class="sortable">
      <tr>
        <th>Name</th>
        <th>Image</th>
        <th>Tier</th>
        <th>Pool</th>
        <th>Description</th>
        <th>Flavor</th>
      </tr>
      {% for relic in mod.relics %}
      <tr>
        <td><a href="../{{ relic.relPopupImage }}">{{ relic.name }}</a></td>
        <td><img src="../{{ relic.relImage }}" width="128" height="128"><a href="../{{ relic.relPopupImage }}"><img src="../{{ relic.relSmallPopupImage }}" class="popup"></a></td>
        <td>{{ relic.tier }}</td>
        <td>{{ relic.pool }}</td>
        <td>{{ relic.descriptionHTML }}</td>
        <td>{{ relic.flavorText }}</td>
      </tr>
      {% endfor %}
    </table>
    {% endif %}

    {% if mod.potions %}
    <h1>{{mod.name}} potions</h1>
    <table class="sortable">
      <tr>
        <th>Name</th>
        <th>Image</th>
        <th>Rarity</th>
        <th>Description</th>
      </tr>
      {% for potion in mod.potions %}
      <tr>
        <td>{{ potion.name }}</td>
        <td><img src="../{{ potion.relImage }}"></td>
        <td>{{ potion.rarity }}</td>
        <td>{{ potion.descriptionHTML }}</td>
      </tr>
      {% endfor %}
    </table>
    {% endif %}

    {% if mod.creatures %}
    <h1>{{mod.name}} creatures</h1>
    <table class="sortable">
      <tr>
        <th>Name</th>
        <th>Image</th>
      </tr>
      {% for creature in mod.creatures %}
      <tr>
        <td>{{ creature.name }}</td>
        <td><img src="../{{ creature.relImage }}"></td>
      </tr>
      {% endfor %}
    </table>
    {% endif %}
  </body>
</html>